name: CI
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  Test-Python-3-8-10-TF-2-6-1:
    runs-on: ubuntu-latest
    container: tensorflow/tensorflow:2.6.1
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - run: python -V
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Test with unittest
        run: |
          python -m unittest
      - run: echo "🍏 This job's status is ${{ job.status }}."
  Test-Python-3-TF-2-8-4:
    runs-on: ubuntu-latest
    container: tensorflow/tensorflow:2.8.4
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: python -V
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Test with unittest
        run: |
          python -m unittest
      - run: echo "🍏 This job's status is ${{ job.status }}."
  Test-Python-3-TF-2-11-1:
    runs-on: ubuntu-latest
    container: tensorflow/tensorflow:2.11.1
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: python -V
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Test with unittest
        run: |
          python -m unittest
      - run: echo "🍏 This job's status is ${{ job.status }}."
  Test-Python-3-11-0rc1-TF-2-16-1:
    runs-on: ubuntu-latest
    container: tensorflow/tensorflow:2.16.1
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: python -V
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Test with unittest
        run: |
          python -m unittest
      - run: echo "🍏 This job's status is ${{ job.status }}."